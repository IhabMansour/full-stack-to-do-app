{
  "name": "todo",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "if-env TYPE=server && (cd server && npm install) || (cd to-do-web && npm install)",
    "server": "node ./server/server.js",
    "web": "cd to-do-web && npm start"
  },
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
